{
  "name": "alba-trading-website",
  "version": "1.0.0",
  "description": "AlbA Trading Co., Ltd — Construction Materials Wholesale, Myanmar",
  "private": true,
  "scripts": {
    "dev":   "npm --prefix artifacts/website install --silent && npm --prefix artifacts/website run dev",
    "build": "npm --prefix artifacts/website install --silent && npm --prefix artifacts/website run build",
    "preview": "npm --prefix artifacts/website run preview",
    "typecheck": "npm --prefix artifacts/website run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
